# Copy to .env.local and fill in. Values come from Supabase → Project Settings → API.
# The publishable key is safe in a client bundle (RLS enforces access, not the key).
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
