// Placeholder — post-auth shell (sidebar + header) implemented in Step 5
export function Dashboard() {
  return (
    <div className="min-h-screen bg-dark-200 flex items-center justify-center">
      <p className="font-sans text-white/50">Dashboard — Step 5</p>
    </div>
  )
}
